name: Build module and deploy to Cloudfront

on:
  push:
    branches:
      - "master"

jobs:
  setup:
    uses: dev-alianwar/ci-cd-example/.github/workflows/setup.yml@master
    with:
      secrets: inherit

  build_module:
    needs: setup
    uses: dev-alianwar/ci-cd-example/.github/workflows/build-module.yml@master
    with:
      dry_run: false
      is_master: true
    secrets: inherit

  deploy-skeleton:
    needs: [setup, build_module]
    uses: dev-alianwar/ci-cd-example/.github/workflows/deploy.yml@master
    with:
      is_master: true
      new_package_version: ${{ needs.build_module.outputs.new_package_version }}
    secrets: inherit
